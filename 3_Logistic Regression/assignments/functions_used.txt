def plotData(X,y) --> scatter plot

def sigmoid(z) --> return g

def costFunction(theta, X, y): --> return J and gradient(theta)

def predict(theta, X): --> return probability (either 1 or 0)

def costFunctionReg(theta, X, y, lambda_): --> return J and gradient(theta)

